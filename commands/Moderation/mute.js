const Enmap = require('enmap');
const discord = require('discord.js')
const { color } = require('../../config.json');

module.exports = {
    name: "mute",
    aliases: [],
    description: "Команда, чтобы замутить пользователя на сервере",
    category: "Moderation",
    cooldown: 0,
    run: async (client, message, args) => {
      // commands starts here
    }
};