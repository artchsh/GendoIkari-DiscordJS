const Enmap = require('enmap');
const discord = require('discord.js')
const { color } = require('../../config.json');

module.exports = {
    name: "kick",
    aliases: [],
    description: "Команда, чтобы кикнуть пользователя с сервера",
    category: "Moderation",
    cooldown: 0,
    run: async (client, message, args) => {
      // commands starts here
    }
};